# CashTimeline — environment variables template
# Copy to .env and fill in your real values.

APP_NAME=CashTimeline
APP_ENV=local
APP_URL=http://localhost/cashtimeline/public

# Database Configuration
DB_HOST=localhost
DB_NAME=cashtimeline
DB_USER=root
DB_PASS=
DB_CHARSET=utf8mb4

# Session
SESSION_NAME=cashtimeline_session

# Email Configuration (SMTP)
# For production: use your production SMTP server
# For Gmail: Enable 2FA and generate an app password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_ENCRYPTION=tls
SMTP_USERNAME=your-email@example.com
SMTP_PASSWORD=your-password-or-app-password
SMTP_FROM_EMAIL=your-email@example.com
SMTP_FROM_NAME=CashTimeline
SMTP_REPLY_TO_EMAIL=support@example.com
SMTP_REPLY_TO_NAME=CashTimeline Support
EMAIL_DEBUG_MODE=false

# SMS Configuration (Twilio)
# Sign up at https://www.twilio.com to get credentials
TWILIO_ACCOUNT_SID=your-account-sid
TWILIO_AUTH_TOKEN=your-auth-token
TWILIO_PHONE_NUMBER=+1234567890
SMS_DEFAULT_COUNTRY_CODE=+1
SMS_DEBUG_MODE=false
